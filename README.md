In order for the app to be run and predict ride wait times, you must train the models using the rf_model.py script. From there, it will take each ride's data and train a random forest model specific to the ride. Once this is done, you can run stream_app.py. 

Hope you enjoy!
